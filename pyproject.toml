[project]
name = "job-search"
version = "0.1.0"
description = ""
authors = [
    {name = "hel1os999",email = "hel1osmif@gmail.com"}
]
requires-python = ">=3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
